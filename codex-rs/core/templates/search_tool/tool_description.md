# Apps（连接器）工具发现

使用 BM25 搜索 apps/连接器的工具元数据，并为下一次模型调用暴露匹配的工具。

你可以访问以下 apps/连接器的所有工具：
{{app_descriptions}}
部分工具可能没有预先提供给你，你应该使用此工具（`tool_search`）来搜索所需工具并为上述 apps 加载它们。对于上述 apps，始终使用 `tool_search` 进行工具发现，而不是使用 `list_mcp_resources` 或 `list_mcp_resource_templates`。
 # Apps (Connectors) 工具发现
 
 使用 BM25 搜索 apps/connectors 的工具元数据，并为下一次模型调用暴露匹配的工具。
 
 你可以访问以下 apps/connectors 的所有工具：
 {{app_descriptions}}
 部分工具可能没有预先提供给你，你应该使用此工具（`tool_search`）搜索所需的工具并为上述 apps 加载它们。对于上述 apps，始终使用 `tool_search` 而不是 `list_mcp_resources` 或 `list_mcp_resource_templates` 进行工具发现。
# 应用（连接器）工具发现

使用 BM25 搜索应用/连接器的工具元数据，并将匹配的工具暴露给下一次模型调用。

你可以使用以下应用/连接器的所有工具：
{{app_descriptions}}
部分工具可能未提前提供给你，你应该使用此工具（`tool_search`）搜索所需的工具并为上述应用加载它们。对于上述应用，始终使用 `tool_search` 而非 `list_mcp_resources` 或 `list_mcp_resource_templates` 进行工具发现。
